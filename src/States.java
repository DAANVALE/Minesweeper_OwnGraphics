public class States {

    public enum mineSweeper { EMPTY, NEARMINE, MINE }

    public enum cell { COVERED, UNCOVERED, FLAGGED }

    public enum game { PLAYNG, WON, LOST }

    public enum dificulty { EASY, MEDIUM, HARD }
}
